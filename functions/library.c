#include<stdio.h>
#include<math.h>
int main(){
    // int a;
    // scanf("%d",&a);
    // int root = sqrt(a);
    // printf("the sqaure root is: %d",root);
    // return 0;
    int power = pow(2,5);
    printf("The number is:%d",power);
    return 0;
}